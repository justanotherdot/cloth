{
  "name": "cloth",
  "private": true,
  "workspaces": [
    "cloth-frontend"
  ],
  "scripts": {
    "dev": "./bin/dev",
    "dev:docker": "./bin/dev-docker",
    "build": "cd cloth-api && worker-build --release && cd ../cloth-frontend && npm run build", 
    "deploy": "./bin/deploy",
    "test": "./bin/test",
    "test:e2e": "cd tests/e2e && npm test"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}